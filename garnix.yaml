servers:
  - configuration: push_notifications_service_provider1
    deployment:
      type: on-branch
      branch: main-0.5
  - configuration: push_notifications_service_provider2
    deployment:
      type: on-branch
      branch: main-0.5
builds:
  exclude:
    - "checks.*.send-push-notification-test"
